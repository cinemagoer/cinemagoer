version: 2

build:
  os: ubuntu-22.04
  image: latest
  tools:
    python: "3.11"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - method: pip
      path: .
    - method: pip
      packages:
        - sphinx>=8.1.3
        - sphinx-rtd-theme>=3.0.2
